name: test

on:
  pull_request:
    branches: [master]
jobs:
  job1:
    name: install package
    runs-on: ubuntu-latest
    steps:
      - run: yarn install
      - run: cd /dSound/dSound && yarn test
      - run: yarn lint
